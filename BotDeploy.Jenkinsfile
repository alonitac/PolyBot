properties([parameters([string('BOT_IMAGE_NAME')])])

pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                sh '''
                echo "deploying"
                '''
            }
        }
    }
}